# P11 · 验收标准 · Scoop 配置页

> 文档层级:页面级契约(qa 产出 · 可执行验收标准,尚未执行)
> 适用范围:web 端 scoop-gui 模块 P11
> 上游依赖:`docs/prd/pages/web/scoop-gui/P11-Scoop配置页.md`
> 真相源:`src/pages/ConfigView.tsx`、`src/scoopConfig.ts`、`src/store.ts`(scoopConfig 状态与动作)、`src/i18n.ts`、`src-tauri/src/commands.rs`(`scoop_config_*`)、`src-tauri/src/config.rs`(如本文验收标准与页面 PRD 字面描述有出入,以组件真实实现为准;第 7 节据实登记出入)
> 关联功能:F20(Scoop 配置可视化编辑)、F19(错误处理与友好提示)

---

## 1. 前置条件

- 本机已完成首次启动流程,Boot Sequence 处于"进入主界面"(`Scoop 自身 = 可用`、`UI Language ≠ 未选择`)。
- 从 P03 侧边栏"Scoop 配置"导航项进入 P11(`view: "config"`)。
- 验收环境可读取 scoop 配置文件 `%USERPROFILE%\.config\scoop\config.json`(`XDG_CONFIG_HOME` 优先),且 `scoop config` 命令可用。

## 2. 用例组 A · 配置项读取与呈现(F20 读)

| 用例 ID | 用例描述 | 验证方法 | 期望结果 |
| --- | --- | --- | --- |
| A1 | 从 P03 可进入 P11 | 点击侧边栏"Scoop 配置" | 主区切换为 P11,该导航项呈选中态,壳层保持可见 |
| A2 | 分区完整 | 检视页面分区 | 恰有 6 个分区:下载器、网络与代理、更新策略、安装与工具、目录、令牌与密钥,顺序固定 |
| A3 | 覆盖全部配置项 | 清点各分区配置项 | 覆盖 `scoop config` 全部 32 项(见 `src/scoopConfig.ts` / api-doc §2 `CONFIG_KEYS`),无遗漏、无重复 |
| A4 | 当前值正确回填 | 在已设置若干项的环境进入 | 各项控件显示 `config.json` 中的当前值(开关态、枚举选中、输入框文本);未设置项以默认值占位,不显示为已设置 |
| A5 | 控件形态匹配类型 | 逐项检视控件 | 布尔→开关;枚举(scoop_branch/default_architecture/shim)→候选值段选按钮;数值→数字输入;字符串→文本输入;令牌→密码输入;private_hosts→只读展示 |
| A6 | 每项展示键名与说明 | 检视任一项 | 展示业务名称 + 一句说明 + 原始键名(如 `aria2-enabled`);键名可选中复制 |
| A7 | 读取失败提示 | 令 `scoop_config_get` 失败(如配置文件不可读) | 页面给出"配置读取失败"业务提示,不白屏、不抛未捕获错误 |

## 3. 用例组 B · 即时写入(F20 写)

| 用例 ID | 用例描述 | 验证方法 | 期望结果 |
| --- | --- | --- | --- |
| B1 | 开关即时写入 | 拨动某布尔项(如 aria2-enabled) | 立即 `scoop config <k> true/false`;开关反映新态;该项随后出现"恢复默认" |
| B2 | 枚举即时写入 | 点击枚举项另一候选(如 scoop_branch → develop) | 立即写入,选中值高亮切换 |
| B3 | 文本/数值失焦写入 | 在输入框改值后失焦或回车 | 以新值即时写入;未变化时不重复写入 |
| B4 | 清空即恢复默认 | 将已设置文本项清空后失焦 | 触发 `scoop config rm <k>`,该项回到默认态 |
| B5 | 无独立"保存"按钮 | 检视页面 | 不存在整页"保存/应用"按钮;每项独立即时生效 |
| B6 | 写入失败回滚 | 令 `scoop_config_set` 失败 | 该项回滚到写入前的值,出现失败提示(`config.saveFailed`),不停留在错误的乐观态 |
| B7 | 写入不入任务队列 | 改动任一项后检视 P08 任务面板 | 不产生任何 InstallJob;配置写入与 F17 长时任务体系完全解耦 |

## 4. 用例组 C · 目录类危险项确认

| 用例 ID | 用例描述 | 验证方法 | 期望结果 |
| --- | --- | --- | --- |
| C1 | 危险项标记 | 检视目录三项(root/global/cache_path) | 每项带"谨慎"类标记,区别于普通项 |
| C2 | 修改前确认 | 修改某目录项并失焦 | 弹出确认框,说明仅改配置、不搬移已装数据的风险;未确认前不写入 |
| C3 | 确认后写入 | 在确认框点"仍然修改" | 写入生效 |
| C4 | 取消不写入 | 在确认框点"取消" | 不写入,保持原值 |
| C5 | 危险项恢复默认也确认 | 对已设置目录项点"恢复默认" | 同样先弹确认,确认后才 `scoop config rm` |

## 5. 用例组 D · 敏感令牌

| 用例 ID | 用例描述 | 验证方法 | 期望结果 |
| --- | --- | --- | --- |
| D1 | 默认遮罩 | 检视 gh_token / virustotal_api_key 输入 | 默认以密码形态遮罩显示 |
| D2 | 可切换显示 | 点击显示/隐藏切换 | 可在明文与遮罩间切换 |
| D3 | 不外传/不落日志 | 检视写入路径 | 令牌仅经 `scoop config` 写入本机;不出现在任何网络请求或应用日志中 |

## 6. 用例组 E · 页面范围与硬约束

| 用例 ID | 用例描述 | 验证方法 | 期望结果 |
| --- | --- | --- | --- |
| E1 | 与 P09 解耦 | 在 P11 改配置后检视 P09 | 不影响产品自有的语言/主题/安装配置回显 |
| E2 | 不影响业务状态 | 改配置前后比对已装列表/桶/任务 | App / Bucket / InstallJob 状态机不受影响 |
| E3 | 壳内路由态 | 进入 P11 后观察窗口结构 | 侧边栏与壳层保持存在可导航;P11 渲染于主区 `<main>`,非全屏 Dialog |
| E4 | 双语齐备 | 切换界面语言 | 分区标题、配置项名称与说明随语言切换;无仅单语文案(项目硬约束) |
| E5 | 复杂项降级 | 检视 private_hosts | 只读展示当前值 + 提示走命令行编辑,不提供易出错的图形编辑 |

## 7. 与页面 PRD 的口径差异登记(据实登记,不改写 PRD 原文)

1. **private_hosts 采用只读降级。** 页面 PRD 场景描述"覆盖全部配置项",实现中 `private_hosts`(对象数组)因经 `scoop config` CLI 传复杂结构易出错,采用只读展示 + 提示命令行编辑(见 `scoopConfig.ts` 中 `control: "readonly"`),其余 31 项完整可视化读写。用例 A5/E5 按此实现口径。
2. **写入值格式交由 scoop 处理。** 前端统一以字符串经 `scoop config <k> <v>` 写入,布尔/数值的类型落库由 scoop 负责(见 api-doc 决策 API03),前端读回时按 JSON 原值展示。

## 8. 文档范围声明

本文件只回答"P11 应通过哪些可观察、可验证的用例",供 qa 阶段执行验收记录 pass/fail 时使用,本身不含执行结果。不涉及:业务流程与状态机定义(唯一出处 `docs/prd/flows/scoop-gui.md`)、功能与页面清单的模块级归属(唯一出处 `docs/prd/modules/scoop-gui.md`)、IPC 契约的完整字段定义(唯一出处 `docs/architecture/api/scoop-gui.md`)。
